{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "check:api-routes": "node scripts/check-openapi-routes.mjs",
    "format:check": "prettier --check \"artifacts/*/src/**/*.{ts,tsx,js,jsx,json,css,md}\" \"lib/*/src/**/*.{ts,tsx,js,json}\" \"scripts/**/*.{ts,js,mjs}\"",
    "format:fix": "prettier --write \"artifacts/*/src/**/*.{ts,tsx,js,jsx,json,css,md}\" \"lib/*/src/**/*.{ts,tsx,js,json}\" \"scripts/**/*.{ts,js,mjs}\"",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~6.0.3",
    "vitest": "^3.2.4"
  }
}
