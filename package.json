{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "check:api-routes": "node scripts/check-openapi-routes.mjs",
    "format:check": "prettier --write \"artifacts/ai-chat-space/src/components/chat/file-generation-panel.tsx\" \"artifacts/ai-chat-space/src/components/chat/message-input.tsx\" && git diff -- \"artifacts/ai-chat-space/src/components/chat/file-generation-panel.tsx\" \"artifacts/ai-chat-space/src/components/chat/message-input.tsx\" && exit 1",
    "format:fix": "prettier --write \"artifacts/api-server/src/**/*.{ts,tsx}\" \"artifacts/ai-chat-space/src/**/*.{ts,tsx}\"",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~6.0.3",
    "vitest": "^3.2.4"
  }
}
