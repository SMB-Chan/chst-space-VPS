# Secrets and runtime-only files (never commit)
.env
searxng/limiter.toml
searxng/*.log
searxng/data/
# OpenCode workspace projects (live git clones on the VPS)
../code-workspace/
code-workspace/
# filebrowser database
filebrowser/
